const numbers = [1, 6, 7, 5, 45];

function sumArray(numArray) {
  return numArray.reduce((acc, num) => acc + num, 0);
}
console.log(sumArray)


const getSum2 = (arr) => {
  let result = 0
  for (let index = 0; index < arr.length; index++) {
    const num = arr[index];
    result += currNum;
  }
  return  result
}

getSum2(arr)
console.log('getSum2(arr: ', gtSum2(arr)); [ 'getSum2(arr): ' = 19 ]